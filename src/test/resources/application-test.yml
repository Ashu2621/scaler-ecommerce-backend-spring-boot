spring:
  datasource:
    url: jdbc:h2:mem:ecommerce-test;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  cache:
    type: none

app:
  security:
    rate-limit:
      requests: 10000
  payment:
    reconciliation-delay-ms: 3600000
